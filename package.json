{
  "name": "max-bot-api-ts-extensions",
  "version": "1.0.0",
  "description": "Расширения для @maxhub/max-bot-api: сессии и сцены",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "max",
    "bot",
    "api",
    "sessions",
    "scenes",
    "telegraf-compatible"
  ],
  "author": "Badalyan Vyacheslav",
  "license": "MIT",
  "dependencies": {
    "@maxhub/max-bot-api": "^0.2.1",
    "better-sqlite3": "^9.5.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.9",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "@maxhub/max-bot-api": "^0.2.1"
  },
  "publishConfig": {
  "registry": "https://npm.pkg.github.com"
  }
}

